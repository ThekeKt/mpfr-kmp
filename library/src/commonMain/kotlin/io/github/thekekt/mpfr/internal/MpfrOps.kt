package io.github.thekekt.mpfr.internal

import io.github.thekekt.mpfr.InternalMpfrApi

/* GENERATED by gradlew :library:generateShim from buildSrc .../ShimTables.kt —
 * do not edit by hand (the same table drives native/jni/mpfr_kmp_ops.h and,
 * through it, the switch bodies in mpfr_kmp_jni.c). Dispatch opcode ids. */

@InternalMpfrApi
internal object MpfrBinaryOp {
    const val ADD: Int = 0 // mpfr.h L580 — mpfr_add(rop, x, y, r)
    const val SUB: Int = 1 // mpfr.h L583 — mpfr_sub(rop, x, y, r)
    const val MUL: Int = 2 // mpfr.h L585 — mpfr_mul(rop, x, y, r)
    const val DIV: Int = 3 // mpfr.h L587 — mpfr_div(rop, x, y, r)
    const val POW: Int = 4 // mpfr_pow — mpfr_pow(rop, x, y, r)
    const val ATAN2: Int = 5 // mpfr.h L735 — mpfr_atan2(rop, x, y, r)
    const val MIN: Int = 6 // mpfr_min: NaN propagates verbatim — mpfr_min(rop, x, y, r)
    const val MAX: Int = 7 // mpfr_max: NaN propagates verbatim — mpfr_max(rop, x, y, r)
    const val DIM: Int = 8 // mpfr_dim — mpfr_dim(rop, x, y, r)
    const val HYPOT: Int = 9 // mpfr_hypot — mpfr_hypot(rop, x, y, r)
    const val AGM: Int = 10 // mpfr_agm — mpfr_agm(rop, x, y, r)
    const val FMOD: Int = 11 // mpfr_fmod — mpfr_fmod(rop, x, y, r)
    const val REMAINDER: Int = 12 // mpfr_remainder — mpfr_remainder(rop, x, y, r)
    const val RELDIFF: Int = 13 // mpfr.texi: |op1−op2|/|op1|, not exactly rounded — mpfr_reldiff(rop, x, y, r)
}

@InternalMpfrApi
internal object MpfrFusedOp {
    const val FMA: Int = 0 // mpfr_fma: fused multiply-add — mpfr_fma(rop, x, y, z, r)
    const val FMS: Int = 1 // mpfr_fms: fused minus-multiply — mpfr_fms(rop, x, y, z, r)
}

@InternalMpfrApi
internal object MpfrUnaryOp {
    const val SQR: Int = 0 // mpfr_sqr — mpfr_sqr(rop, x, r)
    const val SQRT: Int = 1 // mpfr_sqrt — mpfr_sqrt(rop, x, r)
    const val CBRT: Int = 2 // mpfr_cbrt — mpfr_cbrt(rop, x, r)
    const val REC_SQRT: Int = 3 // mpfr.h L578 (sanctioned name: reciprocalSqrt) — mpfr_rec_sqrt(rop, x, r)
    const val EXP: Int = 4 // MPFR manual: exponential functions — mpfr_exp(rop, x, r)
    const val EXP2: Int = 5 // MPFR manual: exponential functions — mpfr_exp2(rop, x, r)
    const val EXP10: Int = 6 // MPFR manual: exponential functions — mpfr_exp10(rop, x, r)
    const val EXPM1: Int = 7 // MPFR manual: near-one exponentials — mpfr_expm1(rop, x, r)
    const val EXP2M1: Int = 8 // MPFR manual: near-one exponentials — mpfr_exp2m1(rop, x, r)
    const val EXP10M1: Int = 9 // MPFR manual: near-one exponentials — mpfr_exp10m1(rop, x, r)
    const val LOG: Int = 10 // MPFR manual: logarithm functions — mpfr_log(rop, x, r)
    const val LOG2: Int = 11 // MPFR manual: logarithm functions — mpfr_log2(rop, x, r)
    const val LOG10: Int = 12 // MPFR manual: logarithm functions — mpfr_log10(rop, x, r)
    const val LOG1P: Int = 13 // MPFR manual: near-one logarithms — mpfr_log1p(rop, x, r)
    const val LOG2P1: Int = 14 // MPFR manual: near-one logarithms — mpfr_log2p1(rop, x, r)
    const val LOG10P1: Int = 15 // MPFR manual: near-one logarithms — mpfr_log10p1(rop, x, r)
    const val SIN: Int = 16 // MPFR manual: trigonometric functions — mpfr_sin(rop, x, r)
    const val COS: Int = 17 // MPFR manual: trigonometric functions — mpfr_cos(rop, x, r)
    const val TAN: Int = 18 // MPFR manual: trigonometric functions — mpfr_tan(rop, x, r)
    const val ASIN: Int = 19 // MPFR manual: inverse trigonometric functions — mpfr_asin(rop, x, r)
    const val ACOS: Int = 20 // MPFR manual: inverse trigonometric functions — mpfr_acos(rop, x, r)
    const val ATAN: Int = 21 // MPFR manual: inverse trigonometric functions — mpfr_atan(rop, x, r)
    const val SINH: Int = 22 // MPFR manual: hyperbolic functions — mpfr_sinh(rop, x, r)
    const val COSH: Int = 23 // MPFR manual: hyperbolic functions — mpfr_cosh(rop, x, r)
    const val TANH: Int = 24 // MPFR manual: hyperbolic functions — mpfr_tanh(rop, x, r)
    const val ASINH: Int = 25 // MPFR manual: inverse hyperbolic functions — mpfr_asinh(rop, x, r)
    const val ACOSH: Int = 26 // MPFR manual: inverse hyperbolic functions — mpfr_acosh(rop, x, r)
    const val ATANH: Int = 27 // MPFR manual: inverse hyperbolic functions — mpfr_atanh(rop, x, r)
    const val SINPI: Int = 28 // MPFR manual: sin(pi*x) family — mpfr_sinpi(rop, x, r)
    const val COSPI: Int = 29 // MPFR manual: sin(pi*x) family — mpfr_cospi(rop, x, r)
    const val TANPI: Int = 30 // MPFR manual: sin(pi*x) family — mpfr_tanpi(rop, x, r)
    const val ERF: Int = 31 // MPFR manual: special functions — mpfr_erf(rop, x, r)
    const val ERFC: Int = 32 // MPFR manual: special functions — mpfr_erfc(rop, x, r)
    const val GAMMA: Int = 33 // MPFR manual: special functions — mpfr_gamma(rop, x, r)
    const val LNGAMMA: Int = 34 // MPFR manual: special functions — mpfr_lngamma(rop, x, r)
    const val DIGAMMA: Int = 35 // MPFR manual: special functions — mpfr_digamma(rop, x, r)
    const val ZETA: Int = 36 // MPFR manual: special functions — mpfr_zeta(rop, x, r)
    const val TRUNC: Int = 37 // MPFR manual: rounding & integer functions (rnd-free) — mpfr_trunc(rop, x)
    const val FLOOR: Int = 38 // MPFR manual: rounding & integer functions (rnd-free) — mpfr_floor(rop, x)
    const val CEIL: Int = 39 // MPFR manual: rounding & integer functions (rnd-free) — mpfr_ceil(rop, x)
    const val ROUND: Int = 40 // MPFR manual: ties away from zero (rnd-free) — mpfr_round(rop, x)
    const val ROUND_EVEN: Int = 41 // mpfr_roundeven: ties to nearest-even — mpfr_roundeven(rop, x)
    const val FRAC: Int = 42 // MPFR manual: fractional part — mpfr_frac(rop, x, r)
    const val NEXTUP: Int = 43 // mpfr_nextabove (x copied into rop first) — mpfr_nextabove(rop)
    const val NEXTDOWN: Int = 44 // mpfr_nextbelow — mpfr_nextbelow(rop)
}

@InternalMpfrApi
internal object MpfrScalarOp {
    const val ADD: Int = 0 // intmax crossing via exact tmp (scalar results keep receiver precision) — mpfr_set_sj(tmp, (intmax_t) k, MPFR_RNDN); mpfr_add(rop, x, tmp, r)
    const val SUB: Int = 1 // intmax crossing via exact tmp — mpfr_set_sj(tmp, (intmax_t) k, MPFR_RNDN); mpfr_sub(rop, x, tmp, r)
    const val MUL: Int = 2 // intmax crossing via exact tmp — mpfr_set_sj(tmp, (intmax_t) k, MPFR_RNDN); mpfr_mul(rop, x, tmp, r)
    const val DIV: Int = 3 // intmax crossing via exact tmp; k=0 → MPFR ±Inf/NaN + DIVBY0 — mpfr_set_sj(tmp, (intmax_t) k, MPFR_RNDN); mpfr_div(rop, x, tmp, r)
    const val MUL_2EXP: Int = 4 // power-of-two scaling (n≥0 by Kotlin contract) — mpfr_mul_2ui(rop, x, (unsigned long) k, r)
    const val DIV_2EXP: Int = 5 // power-of-two scaling (n≥0 by Kotlin contract) — mpfr_div_2ui(rop, x, (unsigned long) k, r)
    const val ROOTN: Int = 6 // mpfr_rootn_ui: n-th root — mpfr_rootn_ui(rop, x, (unsigned long) k, r)
    const val COMPOUND: Int = 7 // mpfr_compound_si: (1+x)^k — mpfr_compound_si(rop, x, (long) k, r)
}

@InternalMpfrApi
internal object MpfrDoubleOp {
    const val ADD: Int = 0 // mixed double, exact tmp — mpfr_set_d(tmp, d, MPFR_RNDN); mpfr_add(rop, x, tmp, r)
    const val SUB: Int = 1 // mixed double, exact tmp — mpfr_set_d(tmp, d, MPFR_RNDN); mpfr_sub(rop, x, tmp, r)
    const val MUL: Int = 2 // mixed double, exact tmp — mpfr_set_d(tmp, d, MPFR_RNDN); mpfr_mul(rop, x, tmp, r)
    const val DIV: Int = 3 // mixed double, exact tmp — mpfr_set_d(tmp, d, MPFR_RNDN); mpfr_div(rop, x, tmp, r)
}

@InternalMpfrApi
internal object MpfrPairOp {
    const val MODF: Int = 0 // mpfr_modf: [integer, fraction] — mpfr_modf(rop, rop2, x, r)
    const val SIN_COS: Int = 1 // mpfr_sin_cos: first output is sin — mpfr_sin_cos(rop, rop2, x, r)
}

@InternalMpfrApi
internal object MpfrConstOp {
    const val PI: Int = 0 // mpfr_const_pi (MPFR manual: constants) — mpfr_const_pi(rop, r)
    const val E: Int = 1 // 4.2.1 lacks mpfr_const_e → exp(1) — mpfr_set_ui(tmp, 1, MPFR_RNDN); mpfr_exp(rop, tmp, r)
}

@InternalMpfrApi
internal object MpfrPredicateOp {
    const val EQUAL: Int = 0 // mpfr.h L712 — mpfr_equal_p(x, y)
    const val LESS: Int = 1 // MPFR manual: comparisons — mpfr_less_p(x, y)
    const val LESS_EQUAL: Int = 2 // MPFR manual: comparisons — mpfr_lessequal_p(x, y)
    const val GREATER: Int = 3 // MPFR manual: comparisons — mpfr_greater_p(x, y)
    const val GREATER_EQUAL: Int = 4 // MPFR manual: comparisons — mpfr_greaterequal_p(x, y)
    const val UNORDERED: Int = 5 // MPFR manual: comparisons — mpfr_unordered_p(x, y)
    const val LESS_GREATER: Int = 6 // MPFR manual: comparisons — mpfr_lessgreater_p(x, y)
    const val TOTAL_ORDER: Int = 7 // mpfr.h L834 — mpfr_total_order_p(x, y)
}

@InternalMpfrApi
internal object MpfrFormatStyle {
    const val SCIENTIFIC: Int = 0 // mpfr_snprintf %R family — e
    const val FIXED: Int = 1 // mpfr_snprintf %R family — f
    const val GENERAL: Int = 2 // mpfr_snprintf %R family — g
}

